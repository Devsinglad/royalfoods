import 'package:get/get.dart';

class Dimensions {
  static double screenHeight = Get.context!.height;
  static double screenWidth = Get.context!.width;

  static double pageViewContainer = screenHeight / 3.54;
  static double pageViewTextContainer = screenHeight / 6.5;

  // favorite food dimension
  static double favoriteImageSize = screenHeight / 2.4;

  //Icon Size
  static double IconSize = screenHeight / 26;
  // bottom height
  static double bottomHeight = screenHeight / 6.6;
}
